import { IoMdSettings } from "react-icons/io";

export default function Contact() {
    return (
        <div className="h-screen flex items-center justify-center text-[0.2rem] md:text-[0.8rem] font-bold">
            UNDER CONSTRUCTION <IoMdSettings className="animate-spin"/>
        </div>
    );
}
